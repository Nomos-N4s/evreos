//! T015's spike host. This commit lands the crate and its pinned dependency
//! set only; the host scaffold and the AccessKit front follow.
#![forbid(unsafe_code)]

fn main() {
    println!("n6-chrome-accessibility: dependency scaffold only; the host follows.");
}
